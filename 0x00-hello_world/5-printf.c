#include <stdio.h>
/**
 * main - Display a message and return 0
 *
 * Description: Use printf for display a message to the terminal
 * "with proper grammar, but the outcome is a piece of art," and a new line
 *
 * Return: Always return 0
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
